package org.knp.secureshell.sync

/**
 * Steady-state LAN sync listener on the desktop (Tauri).
 * Must match `FIXED_SYNC_PORT` in `secureshell-pro/src-tauri/src/sync/listener.rs`.
 */
const val DESKTOP_SYNC_LISTENER_PORT: Int = 43951
